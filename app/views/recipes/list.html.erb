<h2>Recipes involving <% :query %> </h2>

<% @data.each do |recipe| %>
  <h3><%= link_to recipe.label, show_recipes_path(recipe.uri) %></h3>
  <%= link_to image_tag(recipe.image), show_recipes_path(recipe.uri) %>
<% end %>
